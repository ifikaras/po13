# Python dependencies for every project hosted in the po13 repository.
# This union is installed at environment-build time so that a Cloud Agent on any
# branch has the packages it needs without a per-project install step.

# --- Value bet scanner (repo root project) ---
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
scipy>=1.11.0
PyYAML>=6.0.0

# --- Kids YouTube video generator (kids-youtube/) ---
pillow>=11.0.0
moviepy>=2.0.0
gTTS>=2.5.0
pydub>=0.25.0
edge-tts>=7.0.0
numpy>=2.0.0

# --- Test runner shared across projects ---
pytest>=8.0.0
