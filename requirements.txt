curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
scipy>=1.11.0
PyYAML>=6.0.0
# Optional LLM backends (agent works offline with --provider demo)
# openai / anthropic SDKs not required — agent uses urllib JSON APIs
